import styles from './PlayerBar.module.css'

const PlayerBar = () => {
    return(
        <div className={styles['player-bar']}>
            PlayerBar
        </div>
    )
}

export default PlayerBar;